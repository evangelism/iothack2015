<Page
    x:Class="CatMonitor.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CatMonitor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.Content>
                <Grid/>
            </CommandBar.Content>
            <AppBarButton Icon="Refresh" Label="appbarbutton" Click="AppBarButton_Click"/>
            <AppBarButton Icon="Save" Label="appbarbutton"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Left" Height="573" Margin="44,10,0,0" VerticalAlignment="Top" Width="267">
            <Image x:Name="image" Height="276"/>
            <ListView x:Name="listView" Height="281" IsEnabled="True" SelectionChanged="listView_SelectionChanged"/>
        </StackPanel>

    </Grid>
</Page>
